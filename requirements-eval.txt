# Install in a separate evaluation environment, not the training container.
# Install a vLLM build supporting the chosen model/GPU separately.
lm-eval==0.4.9.1
math-verify==0.9.0
huggingface-hub>=0.34
PyYAML>=6
# LCB is installed from its official repository; see docs/evaluation.md.
